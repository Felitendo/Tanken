import SwiftUI
import MapKit

/// Placeholder — replaced by the full map implementation (stations, list drawer, detail).
struct MapTabView: View {
    var body: some View {
        Map()
            .ignoresSafeArea(edges: .top)
    }
}
